Weather SOAP WebService Client
==============================
Simple weather SOAP web service client (University project).

## How to run

### Windows & Linux
Open terminal/command line and navigate to root directory.

Now run the command: `java -jar ./dist/Practica4IS.jar`
